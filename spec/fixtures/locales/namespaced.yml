en:
  dry_schema:
    errors:
      comment:
        rules:
          comment_body:
            filled?: "COMMENT can't be blank"
      post:
        rules:
          post_body:
            filled?: "POST can't be blank"
          comment_body:
            filled?: "post comment must be filled"
  dry_validation:
    post:
      rules:
        post_body: "(dry_validation) Post body"